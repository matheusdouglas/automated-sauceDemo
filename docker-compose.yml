version: '3'
services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.46.1-jammy  
    working_dir: /app
    volumes:
      - .:/app
      - allure-results:/app/allure-results # Mapeia o diretório de resultados
      
    command: npx playwright test --reporter=allure-playwright

volumes:
  allure-results:
