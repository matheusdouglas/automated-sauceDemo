version: '3'
services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.46.1-jammy  
    working_dir: /app
    volumes:
      - .:/app
      - ./playwright-report:/app/playwright-report # Montar o volume para salvar o relatório no host
    command: npx playwright test --reporter=html
