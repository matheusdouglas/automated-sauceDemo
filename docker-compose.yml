version: '3'
services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.46.1-jammy  
    working_dir: /app
    volumes:
      - .:/app
    command: >
      bash -c "npx playwright test --reporter=line,allure && allure generate allure-results --clean -o allure-report"
